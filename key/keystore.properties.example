# Copy to `key/keystore.properties` and fill in your real values.
# This file is read by app/build.gradle.kts to sign the release build.
# The filled-in keystore.properties must never be committed (see .gitignore).
# Use the existing PKCS#12 keystore at key/key; do not generate a replacement.

storePassword=
keyAlias=
keyPassword=
